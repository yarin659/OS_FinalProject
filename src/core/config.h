#ifndef OSFINALPROJECT_CONFIG_H
#define OSFINALPROJECT_CONFIG_H

/**
 * Maximum number of vertices in the graph.
 */
#define MAX_VERTICES 15

/**
 *
 */
#define NODE_DRAW_RADIUS 25.f

/**
 * Time between edge jumps in milliseconds (this is linearly interpolated over anyway)
 */
#define JUMP_DURATION_MS 300.0f

/**
 * The time to wait in each vertex in milliseconds (aside from source and destination)
 */
#define VERTEX_WAIT_MS 1000.0f


#endif //OSFINALPROJECT_CONFIG_H
